policies:
  - name: ajtest-sqs-tag-by-account
    resource: aws.sqs
    filters:
      - "'{Owner}'": present
      - type: glob
        key: QueueUrl
        op: glob
        value: "*ajtest"
    actions:
      - type: tag
        key: Owner
        value: '{Owner}'
